name: 02 - Eventos
on:
  push:
  pull_request:
  schedule:
    - cron: '* * * * *' # Ejecuta el workflow cada minuto
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: ShowTrigger
        run: |
          echo "Event name: ${{ github.event_name }}"